spring.application.name=backend

# Database - Neon PostgreSQL
spring.datasource.url=${DB_URL:jdbc:postgresql://ep-lucky-forest-ap73v4qp-pooler.c-7.us-east-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require}
spring.datasource.username=${DB_USERNAME:neondb_owner}
spring.datasource.password=${DB_PASSWORD:npg_iKbGOlF4p2tI}

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# JWT
app.jwt.secret=${JWT_SECRET:letscrackitt_super_secret_key_make_it_long_256bits_minimum}
app.jwt.expiration-ms=86400000

# CORS - Vercel frontend URL
app.cors.allowed-origins=${CORS_ORIGINS:http://localhost:5173}

# Server
server.port=${PORT:8080}